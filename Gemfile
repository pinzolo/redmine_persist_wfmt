gem 'rails-render_hooks', '>= 2.0.0'

group :development, :test do
  gem 'rspec', '~> 2.14.0'
  gem 'rspec-rails'
  gem 'coveralls'
  gem 'pry'
  gem 'pry-rails'
  if RUBY_VERSION >= '2.0.0'
    gem 'pry-byebug'
  else
    gem 'pry-debugger'
  end
end

group :test do
  gem 'poltergeist'
  gem 'launchy'
  gem 'database_cleaner'
end
